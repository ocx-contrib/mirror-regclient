# NOTICE

This repository packages and redistributes upstream
[regsync](https://github.com/regclient/regclient), part of the regclient
project.

The Apache-2.0 license covers the OCX pipeline files authored here. It does
**not** cover upstream-derived assets — the regsync binaries published to
`ocx.sh/regclient/regsync` (Apache-2.0, the regclient authors), the regsync /
regclient names (used for catalog identification under nominative fair use),
and the regclient logo (sourced from the upstream brand asset at
`regclient.org/brand.svg` and used for catalog identification under
nominative fair use).
